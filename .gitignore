# Credentials and secrets — never commit
*.pem
*.key
id_ed25519*
.env
.env.*

# OS / editor noise
Thumbs.db
Desktop.ini
.DS_Store
*.swp

# Node (if a local static server gets added later)
node_modules/

# Python
__pycache__/
*.pyc

# Local tooling state
.claude/
~$*
runner.log
serve_dashboard_log.txt
